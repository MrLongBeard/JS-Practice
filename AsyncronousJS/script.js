//The Event loop in JS
//Microtask queue
//JS + WebBrowser(dev tools + console + sockets + network requestes + HTML DOM + Timer)
//facade functions
//event loop => its job is to check, if there is any function in call stack 
//and global execution context completed so it pull a function from call back queue and push
//it into call stack.
//there is also micro task queue

